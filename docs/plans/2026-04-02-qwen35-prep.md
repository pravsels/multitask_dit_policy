# Qwen3.5 Preparation Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Prepare an isolated branch and local workflow for migrating from the current CLIP-based text/vision encoders toward a newer multimodal stack such as Qwen3.5.

**Architecture:** Keep the first step intentionally narrow: make the worktree usable for iteration and preserve access to shared large local assets (`data`, `weights`, `outputs`) without copying them into the worktree. In parallel, capture the staged migration plan for replacing the CLIP-specific encoder path with a more general multimodal encoder interface.

**Tech Stack:** Git worktrees, Docker, pytest, shell scripts, Python dataclass config system, Hugging Face `transformers`

---

### Task 1: Make local Docker workflow worktree-friendly

**Files:**
- Modify: `docker/run_local.sh`
- Test: `tests/test_docker_run_local.py`

**Step 1: Write the failing test**

```python
def test_run_local_script_supports_separate_source_and_shared_dirs():
    ...
```

**Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/test_docker_run_local.py -v`
Expected: FAIL because the script only mounts paths derived from its own checkout.

**Step 3: Write minimal implementation**

Add environment-variable overrides so:
- `src` and `tests` can come from the active worktree
- `data`, `weights`, and `outputs` can come from a shared repo root

**Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/test_docker_run_local.py -v`
Expected: PASS

### Task 2: Document the staged Qwen migration path

**Files:**
- Create: `docs/plans/2026-04-02-qwen35-encoder-migration.md`

**Step 1: Write a concrete migration design**

Document:
- current CLIP-specific constraints in `src/multitask_dit_policy/model/observation_encoder.py`
- target abstraction for text/vision or unified multimodal encoders
- config compatibility concerns for old checkpoints and existing train YAMLs
- validation and rollout plan

**Step 2: Review for YAGNI**

Keep the prep doc focused on interface boundaries and incremental migration, not full implementation.

### Task 3: Verify prep branch state

**Files:**
- Modify: `.gitignore`

**Step 1: Ensure worktree directory stays ignored**

Confirm `.worktrees/` is ignored so local worktrees do not pollute git status.

**Step 2: Run focused verification**

Run:
- `python3 -m pytest tests/test_docker_run_local.py -v`
- `python3 -m pytest tests/test_category1_submit_script.py tests/test_observation_encoder.py -v`

Expected: PASS
